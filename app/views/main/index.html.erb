<!DOCTYPE html>
<%= react_component('StartPage',
                    sell: @sell,
                    buy: @buy,
                    fromto: @fromto,
                    spread: @spread
                    ) %>
